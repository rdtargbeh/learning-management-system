package learning_management_system.backend.enums;

public enum VisibleTo {
    PUBLIC,
    TENANT_ONLY,
    ENROLLED_ONLY,
    TEACHER_ONLY,
    COURSE_ONLY,
    PRIVATE,
    RECIPIENT_ONLY,
    PEER

}
