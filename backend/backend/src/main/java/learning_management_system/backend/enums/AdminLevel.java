package learning_management_system.backend.enums;

public enum AdminLevel {
    SUPER_ADMIN,   // Full system-wide control
    TENANT_ADMIN,  // Control limited to a tenant
    DEPARTMENT_ADMIN // Control limited to a department
}

